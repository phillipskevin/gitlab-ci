.container
  - if alert || notice
    - if alert
      .alert.alert-danger= alert
    - if notice
      .alert.alert-info= notice

  - if current_user && Runner.count.zero?
    = render 'shared/no_runners'
